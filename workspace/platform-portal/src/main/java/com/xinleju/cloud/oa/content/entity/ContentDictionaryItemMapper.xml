<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinleju.cloud.oa.content.entity.ContentDictionaryItem">
    <!-- 新增 -->
    <insert id="save">
		${value}
	</insert>
    <!-- 修改根据Id -->
    <update id="update">
		${value}
	</update>
    <!-- 伪删除根据Id -->
    <update id="deletePseudoObjectById">
		${value}
	</update>
    <!-- 批量伪删除根据Id -->
    <update id="deletePseudoAllObjectByIds">
		${value}
	</update>
    <!-- 删除根据Id -->
    <delete id="deleteById">
		${value}
	</delete>
    <!-- 批量删除根据Id -->
    <delete id="deleteBatchByIds">
		${value}
	</delete>
    <!-- 获取单个对象，根据Id-->
    <select id="get" resultType="com.xinleju.cloud.oa.content.entity.ContentDictionaryItem">
		${value}
	</select>
    <!-- 获取列表根据Map查询 -->
    <select id="queryList" resultType="com.xinleju.cloud.oa.content.entity.ContentDictionaryItem">
		${value}
	</select>
    <!-- 获取分页根据Map查询 -->
    <select id="queryPageList" resultType="com.xinleju.cloud.oa.content.entity.ContentDictionaryItem">
		${value}
	</select>
    <!-- 获取总记录数 -->
    <select id="queryCount" resultType="java.lang.Integer">
		${value}
	</select>
    <select id="getAllObjectByDictionaryId" resultType="java.lang.Integer">
		   select count(id) from oa_content_dictionary_item where dictionary_id=#{dictionaryId}
	</select>
    <delete id="deleteAllObjectByDictionaryId">
		DELETE from oa_content_dictionary_item where parent_dictionary_id=#{strId};
	</delete>
    <!-- 过滤分页查询 -->
    <select id="getPageData" resultType="java.util.Map" parameterType="java.util.Map">
        SELECT
        t.id,
        t.val,
        t.name,
        t.sort,
        t.dictionary_id as dictionaryId,
        t.create_date as createDate,
        t.update_date as updateDate,
        t.create_person_id as createPersonId,
        t.create_person_name as createPersonName,
        t.update_person_id as updatePersonId,
        t.update_person_name as updatePersonName,
        t.create_org_id as createOrgId,
        t.create_org_name as createOrgName,
        t.create_company_id as createCompanyId,
        t.create_company_name as createCompanyName,
        t.concurrency_version as concurrencyVersion,
        t.tend_id as tendId,
        t.delflag
        FROM
        oa_content_dictionary_item t where t.delflag=0
        <choose>
            <when test="dictionaryId != null and dictionaryId !=''">
                and t.dictionary_id = #{dictionaryId}
            </when>
        </choose>
        order by t.sort
        <choose>
            <when test="start!=null and start!='' and limit!=null and limit!=''">
                limit #{start},#{limit}
            </when>
        </choose>
    </select>
    <!-- 过滤查询条数 -->
    <select id="getPageDataCount" resultType="java.lang.Integer" parameterType="java.util.Map">
        SELECT
        count(t.id)
        FROM
        oa_content_dictionary_item t where t.delflag=0
        <choose>
            <when test="dictionaryId != null and dictionaryId !=''">
                and t.dictionary_id = #{dictionaryId}
            </when>
        </choose>
    </select>
</mapper>