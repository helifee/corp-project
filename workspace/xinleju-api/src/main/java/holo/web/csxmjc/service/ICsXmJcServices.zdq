package holo.web.csxmjc.service;

import java.util.Map;

public interface ICsXmJcServices {

	/**
	 * 〈回写接口〉 〈详细描述〉
	 * 
	 * @author longjiabo
	 * @param bizId
	 *            流程实例ID
	 * @param soCode
	 *            业务对象编码
	 * @param status
	 *            状态
	 * @param endStatus
	 *            结束状态（当status=2时有值）
	 * @param endTime
	 *            结束时间（当status=2时有值）
	 * @param exData
	 *            回写信息
	 * @return 处理信息
	 */
	// 流程会写参数
	String sendFiStatus(String bizId, String soCode, Integer status,
			Integer endStatus, String endTime, Map<String, Object> exData);

	// 判断参数 流程配置分支参数 返回
	Map<String, Object> queryDetail(String bizId, String soCode);

	String sendAiStatus(String bizId, String soCode, Integer status,
			Integer endStatus, String endTime, Map<String, Object> exData);

}
