package com.sitech.sql.parse.node;

public class Check implements Node {
	public NodeToken token;
	
	public Check(NodeToken token) {
		this.token = token;
	}

	public void accept(Visitor v) {
		v.visitCheck(this);

	}

}
