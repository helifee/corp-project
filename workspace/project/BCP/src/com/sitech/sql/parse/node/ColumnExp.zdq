package com.sitech.sql.parse.node;

public class ColumnExp implements Node {
	public NodeList scalaList;
	public NodeList opopList;
	public ColumnExp(NodeList scalaList,NodeList opopList) {
		this.scalaList = scalaList;
		this.opopList=opopList;
	}

	public void accept(Visitor v) {
		v.visitColumnExp(this);

	}

}
