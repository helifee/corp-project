package com.sitech.sql.parse.node;

public class Columns implements Node {
	public NodeList columnList;
	
	public Columns(NodeList columnList) {
		this.columnList = columnList;
	}

	public void accept(Visitor v) {
		v.visitColumns(this);

	}

}
