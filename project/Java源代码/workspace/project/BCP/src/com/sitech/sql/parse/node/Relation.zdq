package com.sitech.sql.parse.node;

public class Relation implements Node {
	public NodeToken token;
	
	public Relation(NodeToken token) {
		this.token = token;
	}

	public void accept(Visitor v) {
		v.visitRelation(this);

	}

}
