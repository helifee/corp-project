package com.sitech.oa.pars.node;

public class SqlStr implements Node {
	public NodeToken sqlStr;
	public SqlStr(NodeToken sqlStr){
		this.sqlStr=sqlStr;
	}
	public void accept(Visitor v) {
		v.visitSqlStr(this);

	}

}
