package com.jzy.hio.utils;

import java.util.HashMap;
import java.util.Map;

/**
 * 
 * 文件夹工具类
 *
 * <p>FileName：DirectoryUtil</p>
 * <p>Author：yifeihu</p>
 * <p>Date：2018年3月24日 上午10:56:18</p>
 * <p>Version 1.0</p>
 */
public class DirectoryUtil {
	
	/**
	 * 文件名字节长度
	 */
	private static final int fileNameByteLength = 100;
	
	/**
	 * 
	 * 校验文件夹名称
	 * 
	 * DirectoryUtil.validateName<BR>
	 * <p>Author：yifeihu</p>
	 * <p>Date：2018年3月24日 上午10:59:08</p>
	 * @param name 文件夹名称
	 * @return result（结果：false || true）；msg（提示信息） 
	 */
	public static Map<String,Object> validateName(String name){
		Map<String,Object> result = new HashMap<String, Object>();
		String validateMgs = null;
		
		//1、文件夹名称长度、特殊字符
		if(StringUtil.getStringByetLength(name) > fileNameByteLength) {
			validateMgs = "名称最长为"+fileNameByteLength+"个字符";
		}else if(StringUtil.isSpecialChar(name)) {
			validateMgs = "名称包含/\\*”?:><|特殊字符";
		}
		
		if(StringUtil.isNotBlank(validateMgs)) {
			result.put("result", false);
			result.put("msg",validateMgs);
		}else {
			result.put("result", true);
			result.put("msg","数据校验通过");
		}
		return result;
	}
	
	public static void main(String[] args) {
		String s = " 验证 项目 fjkf 我没  PP";
		System.out.println(validateName(s));
				
	}

}
