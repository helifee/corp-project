package com.jzy.hio.utils;

import org.springframework.core.annotation.Order;

/**
 * 用于{@link Order}的值,统一管理 <br/>
 * 值越小、初始化顺序越靠前
 * 
 * @author wt.coffee<br/>
 *         2018年3月7日上午9:23:22
 */
public interface OrderValue {
	int INIT_BEAN_JEDIS_POOL = 0;// 初始化jedisPool

	int AOP_LOG = 1;
	int AOP_PREMISSION = 2;
	int AOP_DATASOURCE = 3;// 设置数据源
	int AOP_TRANSACTION = 4;// 开启事务
	int AOP_REDIS = 5;// redis存储
	int PU_SYNC = 6;// 招采同步公司、用户数数据

	/**
	 * 项目第一层拦截器
	 */
	int FILTER_FIRST = -1;//
}
