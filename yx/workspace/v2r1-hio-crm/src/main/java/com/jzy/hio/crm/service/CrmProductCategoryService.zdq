package com.jzy.hio.crm.service;

import java.util.List;

import com.jzy.hio.base.service.BaseService;
import com.jzy.hio.crm.dto.CrmProductCategoryDto;
import com.jzy.hio.entity.CrmProductCategory;
import com.jzy.hio.springmvc.ApiResponse;

/**
 * 产品类别
 * @author 
 *
 */
public interface CrmProductCategoryService extends BaseService<CrmProductCategory> {

	/**
	 * 根据条件查询count
	 * @param productCategory
	 * @return
	 */
	int selectCountProductCategory(CrmProductCategory productCategory);
	
	/**
	 * 查询名字同一级类别名字是否重复
	 * @param pid 类别id
	 * @param name 名称
	 * @param id 修改的id，新增不传
	 * @return
	 */
	boolean selectCateNameCanUse(String pid, String name, String id);
	
	/**
	 * 根据状态查询类别,父级包子级
	 * @param categoryStatus
	 * @return
	 */
	List<CrmProductCategoryDto> selectCateTree(Integer categoryStatus);
	
	/**
	 * 类别保存
	 * @param productCategory
	 * @return
	 */
	ApiResponse<Void> doProductCateGorySave(CrmProductCategory productCategory);
	/**
	 * 修改类别(service内部调用)
	 * @param productCategory
	 * @param productCategoryOld
	 */
	void updateCategory(CrmProductCategory productCategory, CrmProductCategory productCategoryOld);
	
}
