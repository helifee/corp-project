package com.jzy.hio.sys.portal.mapper;

import java.util.List;
import java.util.Map;

import com.jzy.hio.base.dao.BaseMapper;
import com.jzy.hio.base.utils.Page;
import com.jzy.hio.sys.portal.entity.Component;

/**
 * @author admin
 */

public interface ComponentDao extends BaseMapper<Component> {

	List<Component> queryFuzzyList(Map map);

	Integer queryFuzzyCount(Map map);

	Component getComponentBySerialNo(Component param);

	public List<Map<String, Object>> queryAllList();

	/**
	 * 获取分页数据
	 * 
	 * @param map
	 *            前台参数组织格式： { start:0, limit:20,
	 *            fuzzyQueryFields:JSON.stringify(['name','code']),
	 *            sortFields:JSON.stringify({"sortNum":"asc"}, name:'xxx',
	 *            code:'xxx', ........其他查询条件 }
	 * @return page分页对象
	 */
	public Page queryObjectsByPage(Map map);
}
