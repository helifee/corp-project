package com.jzy.hio.platform.constant;

/**
 * 修改手机号配置
 * @author
 *
 */
public interface PhoneNoSettingConstant {

	/**
	 * 有效时间秒数 7200秒 (60秒 *120分)
	 */
	int EXPIRE_SECONDS = 7200;
	
	/**
	 * 间隔时间60秒，在此时间时间范围内不重新生成验证码 
	 */
	int INTERVAL_SECONDS = 60;
	
	/**
	 * 验证码900秒以内有效(60秒 *15分)
	 */
	int VERIFYCODE_EXPIRE_SECONDS = 900;
	
}
