<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jzy.hio.platform.tenantcompanyinfo.mapper.TenantCompanyInfoMapper">

    <select id="getTenantCompanyInfoByCeatePersonId" resultType="int">
        select count(1)
        from tenant_company_info t
        where t.create_person_id = #{createPersonId}
        and IFNULL(t.delflag,0)=0
    </select>

    <update id="updateDelflag" parameterType="java.lang.String">
        UPDATE tenant_company_info
        SET delflag = 1
        WHERE   tend_id = #{tendId}
    </update>

    <select id="getTenantCompanyInfoByName" resultType="int">
        select count(1)
        from tenant_company_info t
        where t.name = #{name}
        and IFNULL(t.delflag,0)=0
    </select>
</mapper>