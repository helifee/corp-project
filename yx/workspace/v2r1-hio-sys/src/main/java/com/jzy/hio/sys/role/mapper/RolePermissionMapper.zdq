package com.jzy.hio.sys.role.mapper;

import java.util.List;

import org.apache.ibatis.annotations.Param;

import com.jzy.hio.base.mapper.BaseMapper;
import com.jzy.hio.entity.OaSysRolePermission;
import com.jzy.hio.sys.role.dto.RolePermissionDto;
import com.jzy.hio.sys.role.dto.RolePermissionTreeDto;

public interface RolePermissionMapper extends BaseMapper<OaSysRolePermission> {

	public int deleteByRoleId(@Param("roleId") String roleId);
	
	/**
	 * 查询所有的菜单、以及菜单对应的角色权限
	 * 
	 * @param roleId
	 * @return
	 */
	public List<RolePermissionTreeDto> queryMenuAndRolePermission(String roleId);
	
	public List<RolePermissionDto> queryRolePermission(String roleId);

	public List<RolePermissionDto> queryHrRolePermission(@Param("userId") String userId);
	
	/**
	 * 查询该权限是否存在、保证数据唯一性-防止脏数据
	 * 
	 * @param item
	 * @return 返回ID
	 */
	public String queryIsExist(OaSysRolePermission item);
}
