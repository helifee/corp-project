package com.jzy.hio.sys.common.controller;

import java.util.Date;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.jzy.hio.annotation.ResponseBodyExt;
import com.jzy.hio.base.controller.BaseSysController;

/**
 * 系统通用接口
 * 
 * @author wt.coffee<br/>
 *         2018年3月26日上午10:16:57
 */
@RestController
@RequestMapping("common")
public class CommonController extends BaseSysController {

	/**
	 * 返回服务器时间
	 * 
	 * @return
	 */
	@GetMapping("getSysTime")
	@ResponseBodyExt
	public Date getSysTime() {
		return new Date();
	}

}
