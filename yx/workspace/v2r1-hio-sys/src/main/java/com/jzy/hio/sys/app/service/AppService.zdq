package com.jzy.hio.sys.app.service;

import java.util.List;

import com.jzy.hio.base.service.BaseSysService;
import com.jzy.hio.entity.OaSysApp;
import com.jzy.hio.exception.HioException;
import com.jzy.hio.sys.app.dto.AppImDto;
import com.jzy.hio.sys.app.dto.AppItemDto;

/**
 * 应用管理
 * 
 * @author wt.coffee<br/>
 *         2018年3月5日下午7:26:36
 */
public interface AppService extends BaseSysService<OaSysApp> {

	/**
	 * 禁用-启用 app
	 */
	public int updateStatus(OaSysApp app);

	/**
	 * 查询所有的应用列表--系统管理后台
	 * 
	 * @return
	 * @throws HioException
	 */
	public List<AppItemDto> queryList() throws HioException;

	/**
	 * 查询指定用户具备权限的app列表
	 * 
	 * @param userId
	 * @param isAppOrH5
	 *            移动端 true, h5、false
	 * @return
	 */
	public List<AppImDto> queryListByRoleIM(String tendId, String userId);
	
	public List<AppItemDto> queryListByRoleH5(String tendId, String userId);

	/**
	 * h5--查询首页顶部菜单
	 * 
	 * @param userId
	 * @return
	 */
	public List<AppItemDto> queryListByUserId(String userId);
}
