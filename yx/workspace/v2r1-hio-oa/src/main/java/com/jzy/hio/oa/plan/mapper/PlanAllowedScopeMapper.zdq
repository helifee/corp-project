package com.jzy.hio.oa.plan.mapper;

import com.jzy.hio.base.mapper.BaseMapper;
import com.jzy.hio.entity.PlanAllowedScope;

public interface PlanAllowedScopeMapper extends BaseMapper<PlanAllowedScope> {

	/**
	 * 删除计划的共享范围
	 * @param planId
	 */
	void deletePlanScope(String planId);
}