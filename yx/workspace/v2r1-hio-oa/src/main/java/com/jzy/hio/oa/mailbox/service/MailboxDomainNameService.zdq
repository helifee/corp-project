package com.jzy.hio.oa.mailbox.service;

import com.jzy.hio.base.service.BaseService;
import com.jzy.hio.oa.mailbox.entity.MailboxDomainName;
import com.jzy.hio.oa.mailbox.param.MailboxAccountAndDomainNameParam;

/**
 * 邮箱域名 Service 接口
 *
 * @author haoqipeng 2018-05-08
 */
public interface MailboxDomainNameService extends BaseService<MailboxDomainName> {

    /**
     * 保存邮箱账号（带域名设置）
     *
     * @param param 邮箱账号信息（带域名）
     * @return 更新记录数
     */
    int saveOrUpdate(MailboxAccountAndDomainNameParam param);

    /**
     * 检查域名设置是否存在
     *
     * @param domainName 域名
     * @return 存在返回true，否则返回false
     */
    Boolean checkDomainNameExist(String domainName);
}
