package com.jzy.hio.oa.mailbox.service;

import com.jzy.hio.base.service.BaseService;
import com.jzy.hio.oa.mailbox.entity.MailboxAccount;
import com.jzy.hio.oa.mailbox.param.MailboxAccountParam;

/**
 * 邮箱账号 Service 接口
 *
 * @author haoqipeng 2018-05-08
 */
public interface MailboxAccountService extends BaseService<MailboxAccount> {

    /**
     * 保存邮箱账号信息（不含域名设置）
     *
     * @param param 域名信息
     * @return 1-保存成功
     */
    int saveOrUpdate(MailboxAccountParam param);

}
