<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jzy.hio.oa.project.mapper.ProjectStageTaskInfoMapper">

    <select id="queryTaskInfoList" resultType="com.jzy.hio.entity.ProjectStageTaskInfo">
     SELECT
	id
stage_id AS stageId,
stage_group_id AS stageGroupId,
task_id AS taskId,
task_order AS taskOrder
FROM
	oa_project_stage_task_info t
WHERE
	   t.delflag = 0
		<if test="projectStageGroupId != null">
		   and t.stage_group_id = #{projectStageGroupId}
		</if>

		<if test="projectStageId != null">
			and t.stage_id = #{projectStageId}
		</if>
	     order by t.stage_group_order ASC

    </select>



</mapper>