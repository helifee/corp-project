package com.jzy.hio.feign;

import org.springframework.cloud.netflix.feign.FeignClient;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

import com.jzy.hio.ApiFeignResponse;
import com.jzy.hio.sys.user.dto.HrUserPhotoDto;
import com.jzy.hio.sys.user.param.HrUserPhotoParam;

/**
 * Hr人事管理feigen服務
 * @author lixd
 * @create 2018-6-7 09:52:34
 **/
@FeignClient(name = "v2r1-hr-service")
public interface HrFeignService {
	
	/**
	 * 通过im_user_id查询用户头像
	 * @author panbz
	 * @date 2018-6-6 17:20:45
	 * 
	 * @param tendId   租户id
	 * @param imUserId im用户id
	 * @return
	 */
	@RequestMapping(value = "/emp/empPersonInfo/queryUserPhotoByImUserId",method = RequestMethod.POST)
	ApiFeignResponse<HrUserPhotoDto> queryUserPhotoByImUserId(@RequestBody HrUserPhotoParam apiParam);
	
}
