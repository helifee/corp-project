info:
  version: "v2"
  name: "hio"
management:
 security:
  enabled: false #不认证，解决关于springboot-actuator监控的401无权限访问
server:
  port: 8991 #http://localhost:8991/hystrix/
turbine:
  stream:
    port: 8992
spring:
  application:
    name: hio-turbine