<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinleju.cloud.oa.servey.entity.ServeyParty">
	<!-- 新增 -->
	<insert id="save">
		${value}
	</insert>
	<!-- 修改根据Id -->
	<update id="update">
		${value}
	</update>
	<!-- 伪删除根据Id -->
	<update id="deletePseudoObjectById">
		${value}
	</update>
	<!-- 批量伪删除根据Id -->
	<update id="deletePseudoAllObjectByIds">
		${value}
	</update>
	<!-- 删除根据Id -->
	<delete id="deleteById">
		${value}
	</delete>
	<!-- 批量删除根据Id -->
	<delete id="deleteBatchByIds">
		${value}
	</delete>
	<!-- 获取单个对象，根据Id-->
	<select id="get" resultType="com.xinleju.cloud.oa.servey.entity.ServeyParty">
		${value}
	</select>
	<!-- 获取列表根据Map查询 -->
	<select id="queryList" resultType="com.xinleju.cloud.oa.servey.entity.ServeyParty">
		${value}
	</select>
	<!-- 获取分页根据Map查询 -->
	<select id="queryPageList" resultType="com.xinleju.cloud.oa.servey.entity.ServeyParty">
		${value}
	</select>
	<!-- 获取总记录数 -->
	<select id="queryCount" resultType="java.lang.Integer">
		${value}
	</select>
	
	 <!-- 过滤分页查询 -->
    <select id="getServeyInfoPage" resultType="java.util.Map" parameterType="java.util.Map">
        SELECT
        t.id,
        t.create_person_id as createPersonId,
        t.create_person_name as createPersonName,
        t.create_org_id as createOrgId,
        t.create_org_name as createOrgName,
        t.create_company_id as createCompanyId,
        t.create_company_name as createCompanyName,
        t.create_date as createDate,
        t.update_person_id as updatePersonId,
        t.update_person_name as updatePersonName,
        t.update_date as updateDate,
        t.delflag as delflag,
        t.tend_id as tendId,
        t.concurrency_version as concurrencyVersion,
        
        s.name as serveyName,
        s.thru_date as thruDate,
        t.party_type as partyType,
        t.type as type,
        t.servey_id as serveyId,
        c.name as categoryName,
        <![CDATA[CASE WHEN s.thru_date<NOW() THEN '已结束' WHEN s.from_date>NOW() THEN '未开始' ELSE '进行中' END as status ]]>
        FROM
        oa_servey s left join oa_servey_party t on s.id = t.servey_id 
        left join oa_servey_category c on s.servey_category_id = c.id
        where 1=1
        <if test="type!=null and type!=''">
            AND t.type = #{type}
        </if>
         <if test="createPersonId != null and createPersonId !='' ">
            AND t.party_id = #{createPersonId}
        </if>
        <if test="partyName != null and partyName !='' ">
            AND t.party_name = #{partyName}
        </if>
        
        <if test="name != null and name !='' ">
            and s.name like CONCAT(CONCAT('%',#{name}),'%')
        </if>
        and s.status = 2
        order by s.thru_date desc
        <choose>
            <when test="start!=null and start!='' and limit!=null and limit!=''">
                limit #{start},#{limit}
            </when>
        </choose>
       
    </select>
	
	
	 <!-- 过滤查询条数 -->
    <select id="getServeyInfoPageCount" resultType="java.lang.Integer" parameterType="java.util.Map">
        SELECT
        count(t.id)
        FROM
        oa_servey s left join oa_servey_party t on s.id = t.servey_id 
        left join oa_servey_category c on s.servey_category_id = c.id
        where 1=1
        <if test="type!=null and type!=''">
            AND t.type = #{type}
        </if>
        <if test="createPersonId != null and createPersonId !='' ">
            AND t.party_id = #{createPersonId}
        </if>
        <if test="partyName != null and partyName !='' ">
            AND t.party_name = #{partyName}
        </if>
         <if test="name != null and name !='' ">
            and s.name like CONCAT(CONCAT('%',#{name}),'%')
        </if>
    </select>
</mapper>