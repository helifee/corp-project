<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jzy.hio.oa.sys.mapper.SysCommonToolsDao">
	<!-- 新增 -->
	<insert id="save">
		${value}
	</insert>
	<!-- 修改根据Id -->
	<update id="update">
		${value}
	</update>
	<!-- 伪删除根据Id -->
	<update id="deletePseudoObjectById">
		${value}
	</update>
	<!-- 批量伪删除根据Id -->
	<update id="deletePseudoAllObjectByIds">
		${value}
	</update>
	<!-- 删除根据Id -->
	<delete id="deleteById">
		${value}
	</delete>
	<!-- 批量删除根据Id -->
	<delete id="deleteBatchByIds">
		${value}
	</delete>
	<!-- 获取单个对象，根据Id-->
	<select id="get" resultType="com.jzy.hio.oa.sys.entity.SysCommonTools">
		${value}
	</select>
	<!-- 获取列表根据Map查询 -->
	<select id="queryList" resultType="com.jzy.hio.oa.sys.entity.SysCommonTools">
			SELECT
	t.id as id,
	t.code as code,
	t.icon as icon,
	t.name as name,
	t.sort as sort,
	t.url as url,
	t.pic as pic,
	t.concurrency_version,
	t.create_date AS createDate,
	t.update_date AS updateDate,
	t.create_person_id AS createPersonId,
	t.create_person_name AS createPersonName,
	t.update_person_id AS updatePersonId,
	t.update_person_name AS updatePersonName,
	t.create_org_id AS createOrgId,
	t.create_org_name AS createOrgName,
	t.create_company_id AS createCompanyId,
	t.create_company_name AS createCompanyName,
	t.concurrency_version AS concurrencyVersion,
	t.delflag AS delflag
FROM
	oa_sys_common_tools t where t.delflag=0
ORDER BY
	t.sort
	</select>
	<!-- 获取分页根据Map查询 -->
	<select id="queryPageList" resultType="com.jzy.hio.oa.sys.entity.SysCommonTools">
	</select>
	<!-- 获取分页根据Map查询 -->
	<select id="getCommonToolsList" resultType="com.jzy.hio.oa.sys.entity.SysCommonTools">
		SELECT
	t.id as id,
	t.code as code,
	t.icon as icon,
	t.name as name,
	t.sort as sort,
	t.url as url,
	t.pic as pic,
	t.concurrency_version,
	t.create_date AS createDate,
	t.update_date AS updateDate,
	t.create_person_id AS createPersonId,
	t.create_person_name AS createPersonName,
	t.update_person_id AS updatePersonId,
	t.update_person_name AS updatePersonName,
	t.create_org_id AS createOrgId,
	t.create_org_name AS createOrgName,
	t.create_company_id AS createCompanyId,
	t.create_company_name AS createCompanyName,
	t.concurrency_version AS concurrencyVersion,
	t.delflag AS delflag
FROM
	oa_sys_common_tools t where t.delflag=0 ORDER BY t.sort  
<if test="start!=null and start!='' and limit !='' and limit !=null">
  limit #{start},#{limit}
</if>
	</select>
	<!-- 获取总记录数 -->
	<select id="queryCount" resultType="java.lang.Integer">
		${value}
	</select>
	<select id="getCommonToolsCount" resultType="java.lang.Integer">
		select count(t.id) from oa_sys_common_tools t where t.delflag=0
	</select>
	<!-- 获取总记录数 -->
	<select id="getmaxSort" resultType="java.lang.Integer">
		select sort from oa_sys_common_tools t order by t.sort  desc limit 1 
	</select>
</mapper>