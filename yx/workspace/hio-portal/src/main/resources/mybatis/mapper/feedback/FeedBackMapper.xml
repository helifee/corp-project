<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jzy.hio.oa.feedback.mapper.FeedBackDao">
	<!-- 新增 -->
	<insert id="save">
		${value}
	</insert>
	<!-- 修改根据Id -->
	<update id="update">
		${value}
	</update>
	<!-- 伪删除根据Id -->
	<update id="deletePseudoObjectById">
		${value}
	</update>
	<!-- 批量伪删除根据Id -->
	<update id="deletePseudoAllObjectByIds">
		${value}
	</update>
	<!-- 删除根据Id -->
	<delete id="deleteById">
		${value}
	</delete>
	<!-- 批量删除根据Id -->
	<delete id="deleteBatchByIds">
		${value}
	</delete>
	<!-- 获取单个对象，根据Id-->
	<select id="get" resultType="com.jzy.hio.oa.feedback.entity.FeedBack">
		${value}
	</select>
	<!-- 获取列表根据Map查询 -->
	<select id="queryList" resultType="com.jzy.hio.oa.feedback.entity.FeedBack">
		${value}
	</select>
	<select id="getFeedBackPageList" resultType="com.jzy.hio.oa.feedback.entity.FeedBack">
	  	    select 
		 	t.id as id ,
            t.create_date as  createDate,
            t.update_date as updateDate,
            t.create_person_id as createPersonId,
            t.create_person_name as createPersonName,
            t.update_person_id as updatePersonId,
            t.update_person_name as updatePersonName,
            t.create_org_id as createOrgId,
            t.create_org_name as createOrgName,
            t.create_company_id as createCompanyId,
            t.create_company_name as createCompanyName,
            t.concurrency_version as concurrencyVersion,
          	t.delflag as delflag,
            t.theme  as theme,
            t.code as  code,
            t.content as  content,
            t.remark as  remark
			 from oa_sys_feedback t where t.delflag=0 
			 <if test="theme!=null and  theme!=''">
			  and t.theme  LIKE  CONCAT(CONCAT('%',#{theme}),'%')
			 </if>
			 order by t.create_date  desc
           limit #{start},#{limit}
	</select>
	<select id="getFeedBackPageListCount" resultType="java.lang.Integer">
	  	    select 
		 count(t.id)
			 from oa_sys_feedback t where t.delflag=0 
			 <if test="theme!=null and  theme!=''">
			  and t.theme  LIKE  CONCAT(CONCAT('%',#{theme}),'%')
			 </if>
	</select>
	<!-- 获取分页根据Map查询 -->
	<select id="queryPageList" resultType="com.jzy.hio.oa.feedback.entity.FeedBack">
		${value}
	</select>
	<!-- 获取总记录数 -->
	<select id="queryCount" resultType="java.lang.Integer">
		${value}
	</select>
</mapper>