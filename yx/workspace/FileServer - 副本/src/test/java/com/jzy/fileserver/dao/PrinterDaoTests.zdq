package com.jzy.fileserver.dao;

import java.util.List;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.jzy.fileserver.domain.Printer;

@RunWith(SpringRunner.class)
@SpringBootTest
public class PrinterDaoTests {
	
	@Autowired
	private PrinterDao printerDao;

	@Test
	public void contextLoads() {
	}

	@Test
	public void updateTaskNum() {
		printerDao.updateTaskNumById(1, 1);
	}
	
	@Test
	public void save() {
		Printer printer = new Printer();
		printer.setId(2);
		printer.setHost("192.168.3.178");
		printer.setTaskNum(3);
		printerDao.save(printer);
	}
	
	@Test
	public void getFastestPrinter() {
		List<Printer> p = printerDao.getActivePrinters(300);
		System.out.println(p);
	}
	
}
