package com.jzy.fileserver.dao;

import java.util.List;

import org.apache.ibatis.annotations.Param;

import com.jzy.fileserver.domain.Printer;

/**
 * 打印机
 *
 * Created by daoqi on 2018年8月23日
 */
public interface PrinterDao {

	/**
	 * 更新打印机信息
	 * 
	 * @param address
	 * @param taskNum
	 * @return
	 */
	int updateTaskNumById(@Param("id")int id, @Param("taskNum")int taskNum);

	/**
	 * 保存打印机信息
	 * 
	 * @param printer
	 * @return
	 */
	int save(Printer printer);
	
	/**
	 * 取得最快打印机地址
	 * 
	 * @param timeout ： 打印机超时时间（s）
	 * @return
	 */
	List<Printer> getActivePrinters(@Param("timeout")int timeout);
}
