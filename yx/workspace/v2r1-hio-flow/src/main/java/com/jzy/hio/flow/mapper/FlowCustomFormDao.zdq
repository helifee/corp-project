package com.jzy.hio.flow.mapper;

import java.util.List;

import com.jzy.hio.base.mapper.BaseMapper;
import com.jzy.hio.flow.entity.FlowCustomForm;
import com.jzy.hio.flow.entity.param.FlowCustomFormParam;
import com.jzy.hio.flow.entity.param.FlowFormParam;

/**
 * 自定义表单Dao
 * 
 * @author daoqi
 *
 */
public interface FlowCustomFormDao extends BaseMapper<FlowCustomForm> {

	/**
	 * 根据流程模板编码查询自定义表单
	 * 
	 * @param templateCode
	 * @return
	 */
	public List<FlowCustomForm> getCustomFormByTemplateCode(String templateCode);

	/**
	 * 查询自定义表单中流程属性
	 * 
	 * @param flowTemplateCode
	 * @return
	 */
	public String queryFlowPropertiesBy(String flowTemplateCode);

    List<FlowCustomForm> getExpression(FlowFormParam FlowFormParam);

	int updateCustomFormByTemCode(String templateCode);
}
