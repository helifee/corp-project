package com.jzy.hio.flow.service;

import com.jzy.hio.annotation.DataSource;
import com.jzy.hio.flow.entity.dto.ApprovalDto;
import com.jzy.hio.flow.entity.param.ApprovalParam;
import com.jzy.hio.utils.Page;

/**
 * 我的审批记录服务
 * @author chaoyang
 *
 */
public interface FlowInstanceApprovalRecordService {

	/**
	 *  查询我的审批列表
	 * @param approvalDTO
	 * @return
	 */
	Page<ApprovalDto> getMyApprvoalList(ApprovalParam  approvalParam);

	/**
	 * 查询待审，已办数量
	 * 
	 * @param approvalParam
	 * @return
	 */
	int getMyApproveCount(ApprovalParam approvalParam);

	Integer queryMyUnApprovalCount(@DataSource String tendId, String userIdOA);

}
