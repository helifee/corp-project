<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jzy.hio.flow.mapper.FlowTemplateAcCopyDao">
    <update id="updateAcCopyByTemId">
        UPDATE `flow_template_ac_copy`
        SET `delflag` = '1'
        WHERE
            flow_template_id=#{templateId}
    </update>

    <select id="getTemplateAcCopyList" resultType="com.jzy.hio.flow.entity.FlowTemplateAcCopy">
            SELECT
                c.id,
                c.userid,
                c.flow_ac_id AS flowAcId,
                c.flow_template_id AS flowTemplateId,
                c.user_type AS userType,
                c.im_user_id AS imUserId,
                c.delflag
            FROM
                flow_template_ac_copy c LEFT
            JOIN flow_template t ON t.`code` = #{code}
            AND t.delflag = 0
            WHERE
                c.flow_template_id = t.id
    </select>

	<select	id="queryCopyUserIds" resultType="java.lang.String">
		SELECT userId   from flow_template_ac_copy where 	flow_ac_id = #{templateAcId}
	</select>	

</mapper>