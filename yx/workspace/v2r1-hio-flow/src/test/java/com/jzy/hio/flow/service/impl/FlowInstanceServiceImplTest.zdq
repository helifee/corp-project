/*
package com.jzy.hio.flow.service.impl;

import static org.aspectj.bridge.MessageUtil.fail;
import static org.junit.Assert.*;

import java.util.List;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.jzy.hio.HioFlowApplication;
import com.jzy.hio.flow.entity.FlowForm;
import com.jzy.hio.flow.entity.FlowInstanceAc;

@RunWith(SpringRunner.class)
@SpringBootTest(classes = HioFlowApplication.class)
@AutoConfigureMockMvc
public class FlowInstanceServiceImplTest {
	
	@Autowired
	private FlowInstanceServiceImpl flowInstanceService;

	@Test
	public voaid resolveTemplate() {
		String templateId = "";
		List<FlowForm> businessData = null;
		String instanceId = null;
//		List<FlowInstanceAc> list = flowInstanceService.resolveTemplate(templateId, instanceId, businessData);
//		System.out.println(list);
		fail("Not yet implemented");
	}

}
*/
