<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinleju.platform.sys.notice.entity.SysNoticePhoneMsg">
	<!-- 新增 -->
	<insert id="save">
		${value}
	</insert>
	<!-- 修改根据Id -->
	<update id="update">
		${value}
	</update>
	<!-- 伪删除根据Id -->
	<update id="deletePseudoObjectById">
		${value}
	</update>
	<!-- 批量伪删除根据Id -->
	<update id="deletePseudoAllObjectByIds">
		${value}
	</update>
	<!-- 删除根据Id -->
	<delete id="deleteById">
		${value}
	</delete>
	<!-- 批量删除根据Id -->
	<delete id="deleteBatchByIds">
		${value}
	</delete>
	<!-- 获取单个对象，根据Id-->
	<select id="get" resultType="com.xinleju.platform.sys.notice.entity.SysNoticePhoneMsg">
		${value}
	</select>
	<!-- 获取列表根据Map查询 -->
	<select id="queryList" resultType="com.xinleju.platform.sys.notice.entity.SysNoticePhoneMsg">
		${value}
	</select>
	<!-- 获取分页根据Map查询 -->
	<select id="queryPageList" resultType="com.xinleju.platform.sys.notice.entity.SysNoticePhoneMsg">
		${value}
	</select>
	<!-- 获取总记录数 -->
	<select id="queryCount" resultType="java.lang.Integer">
		${value}
	</select>
	<!-- 过滤分页查询 -->
	<select id="getPageData" resultType="java.util.Map" parameterType="java.util.Map">
		SELECT
			t.id,
			t.phones,
			t.msg,
			t.status,
			t.num,
			t.phonel_server_id as phonelServerId,
			s.name as phoneServerName,
			t.template,
			t.is_template as isTemplate,
			t.remark,
			t.create_date as createDate,
			t.update_date as  updateDate,
			t.create_person_id as createPersonId,
			t.create_person_name as createPersonName,
			t.update_person_id as updatePersonId,
			t.update_person_name as updatePersonName,
			t.create_org_id as createOrgId,
			t.create_org_name as createOrgName,
			t.create_company_id as createCompanyId,
			t.create_company_name as createCompanyName,
			t.concurrency_version as concurrencyVersion,
			t.tend_id as tendId,
			t.delflag
		FROM
			pt_sys_notice_phone_msg t left join pt_sys_notice_phone_server s on t.phonel_server_id = s.id where t.delflag=0  
		<choose>
		<when test="phones != null and phones !=''">     
          	and  (t.phones like CONCAT(CONCAT('%',#{phones}),'%')
          		or  t.msg like CONCAT(CONCAT('%',#{msg}),'%')
          		or  t.num  like CONCAT(CONCAT('%',#{num}),'%')
          	)
        </when>
        </choose>
        order by t.create_date desc 
        <choose>
		<when test="start!=null and start!='' and limit!=null and limit!=''">
         limit #{start},#{limit}
        </when>
        </choose>
	</select>
	<!-- 过滤查询条数 -->
	<select id="getPageDataCount" resultType="java.lang.Integer" parameterType="java.util.Map">
		SELECT
			count(t.id)
		FROM
			pt_sys_notice_phone_msg t left join pt_sys_notice_phone_server s on t.phonel_server_id = s.id where t.delflag=0 
		<choose>
		<when test="phones != null and phones !=''">     
          	and  (t.phones like CONCAT(CONCAT('%',#{phones}),'%')
          		or  t.msg like CONCAT(CONCAT('%',#{msg}),'%')
          		or  t.num  like CONCAT(CONCAT('%',#{num}),'%')
          	)
        </when>
        </choose>
	</select>
	<!--修改状态-->
	<update id="updateStatus" parameterType="java.util.Map">
		UPDATE PT_SYS_NOTICE_PHONE_MSG  SET  status = '1' WHERE ID = #{id}
	</update>
</mapper>