package com.jzy.fileserver.dao;

import static org.junit.Assert.assertNotNull;

import java.util.Date;
import java.util.List;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.jzy.fileserver.domain.TaskDetail;

@RunWith(SpringRunner.class)
@SpringBootTest
public class TaskDetailDaoTests {
	
	@Autowired
	private TaskDetailDao taskDetailDao;

	@Test
	public void contextLoads() {
	}

	@Test
	public void save() {
		TaskDetail taskDetail = new TaskDetail();
		taskDetail.setTaskId(1);
		taskDetail.setFileId(1);
		taskDetail.setImageType(0);
		taskDetail.setPageId(2);
		taskDetail.setPath("xx/xx.xx");
		taskDetail.setCreateTime(new Date());
		taskDetailDao.save(taskDetail);
	}
	
	@Test
	public void findByFileId() {
		List<TaskDetail> findByFileId = taskDetailDao.findByFileId(1);
		assertNotNull(findByFileId);
	}
	
}
