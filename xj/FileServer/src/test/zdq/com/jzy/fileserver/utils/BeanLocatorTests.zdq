package com.jzy.fileserver.utils;

import static org.junit.Assert.assertNotNull;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.jzy.fileserver.printer.handler.MsgHandler;
import com.jzy.fileserver.printer.handler.TaskAcceptMsgHandler;

@RunWith(SpringRunner.class)
@SpringBootTest
public class BeanLocatorTests {


	@Test
	public void test() {
		MsgHandler handler = (MsgHandler) BeanLocator.getBean(TaskAcceptMsgHandler.class);
		assertNotNull(handler);
	}
}
