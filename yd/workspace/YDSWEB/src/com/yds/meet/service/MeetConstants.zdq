/*
 * @(#)MeetConstants.java
 * Copyright (c) 2009-2010 大连远东计算机系统有限公司
 * All rights reserved.
 *      Project: 远东公司内部网
 *    SubSystem: 会议室管理
 */

package com.yds.meet.service;

/**
 * 共通定数
 * 
 */
public interface MeetConstants {
	/**
	 * Yc0030迁移需要的逻辑视图.
	 */
	public static final String FROM_YC0020 = "yc0020";
	public static final String FROM_YC0040 = "yc0040";

	/**
	 * 会议申请Flg.
	 */
	public static enum APPLY_FLG {
		/** 没有申请. */
		NO(0),
		/** 有人申请. */
		YES(1);

		private Integer value;

		APPLY_FLG(Integer value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "APPLY_FLG";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public Integer value() {
			return this.value;
		}
	}

	/**
	 * 申请人Flg.
	 */
	public static enum APPLY_USER_FLG {
		/** 不是申请人. */
		NO(0),
		/** 是申请人. */
		YES(1);

		private Integer value;

		APPLY_USER_FLG(Integer value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "APPLY_USER_FLG";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public Integer value() {
			return this.value;
		}
	}

	/**
	 * 会议预约Flg.
	 */
	public static enum RESERVE_FLG {
		/** 日计会议. */
		DAILY("0"),
		/** 周期会议. */
		CYCLE("1");

		private String value;

		RESERVE_FLG(String value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "RESERVE_FLG";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public String value() {
			return this.value;
		}
	}

	/**
	 * 工作日标识.
	 */
	public static enum WORK_DAY_FLG {
		/** 星期一. */
		MON(1),
		/** 星期二. */
		TUE(2),
		/** 星期三. */
		WEN(3),
		/** 星期四. */
		THU(4),
		/** 星期五. */
		FRI(5);

		private Integer value;

		WORK_DAY_FLG(Integer value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "WORK_DAY_FLG";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public Integer value() {
			return this.value;
		}
	}

	/**
	 * 工作日.
	 */
	public static enum WORK_DAY {
		/** 星期一. */
		MON("星期一"),
		/** 星期二. */
		TUE("星期二"),
		/** 星期三. */
		WEN("星期三"),
		/** 星期四. */
		THU("星期四"),
		/** 星期五. */
		FRI("星期五");

		private String value;

		WORK_DAY(String value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "WORK_DAY";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public String value() {
			return this.value;
		}
	}

	/**
	 * 周期会议类型.
	 */
	public static enum CYCLE_FLG {
		/** 每日. */
		DAILY(1),
		/** 每周. */
		WEEKLY(2),
		/** 每月. */
		MONTHLY(3);

		private Integer value;

		CYCLE_FLG(Integer value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "CYCLE_FLG";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public Integer value() {
			return this.value;
		}
	}

	/**
	 * 会议室预约详细画面模式.
	 */
	public static enum VIEW_MODE {
		/** 新建画面. */
		CREATE("0"),
		/** 修改画面. */
		MODIFY("1");

		private String value;

		VIEW_MODE(String value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "VIEW_MODE";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public String value() {
			return this.value;
		}
	}

	/** 公开flg. */
	public static final Integer PUBLICFLG = 1;

	/** 保密显示. */
	public static final String METTOPIC = "保密";

	/** 超时长度. */
	public static final int OUTTIME_LENGTH = 30;

	/** 会议最小时长. */
	public static final int MET_TIME_LENGTH = 30;

	/** 周期内会议天数 每天. */
	public static final String EACH_DAY = "0";

	/** 默认周期ID. */
	public static final Long DEFAULT_CYCLEMET_ID = Long.parseLong("0");

	/**
	 * 日期类型RadioButton.
	 */

	/** 画面RadioButton 当日. */
	public static final String RADIO_BTN_DAY = "currentDay";
	public static final String RADIO_BTN_DAY_NM = "当日";

	/** 画面RadioButton 当周. */
	public static final String RADIO_BTN_WEEK = "currentWeek";
	public static final String RADIO_BTN_WEEK_NM = "当周";

	/** 画面RadioButton 当月. */
	public static final String RADIO_BTN_MONTH = "currentMonth";
	public static final String RADIO_BTN_MONTH_NM = "当月";

	/** 画面RadioButton 指定日期. */
	public static final String RADIO_BTN_SPECIALDAY = "SpecifiedDate";
	public static final String RADIO_BTN_SPECIALDAY_NM = "指定日期";

	/** 单日radioButton显示 */
	public static final Integer RADIO_SINGLE = 0;
	public static final String RADIO_SINGLENM = "单日预约";

	/** 周期radioButton显示 */
	public static final Integer RADIO_WEEK = 1;
	public static final String RADIO_WEEK_NM = "周期预约";

	/**
	 * 画面返回值.
	 */
	public static enum BACK_SIGN {
		/** 保存成功. */
		INSERTOK(0),
		/** 预约时间已过. */
		INSERTOVERTIME(1),
		/** 已有人预约. */
		INSERTCRASH(2),
		/** 预约时间错误. */
		TIMEERROR(3),
		/** 预约的星期不存在. */
		WEEKNOTEXIST(4),
		/** 每月选择的日期不存在. */
		DATE_NOT_EXIST(5),
		/** 修改成功. */
		MODIFYOK(6),
		/** 预约过期，不能修改. */
		DELETE_TIME_OVER(7),
		/** 删除成功. */
		DELETEOK(8), 
		/** 初期进入. */
		INIT(9), 
		/** 没有修改. */
		NOMODIFY(10), 
		/** 没有修改. */
		OVERONEYEAR(11),
		/** 请求的会议不存在. */
		NOTEXIT(12), ;

		private Integer value;

		BACK_SIGN(Integer value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "VIEW_MODE";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public Integer value() {
			return this.value;
		}
	}

	/**
	 * 用户权限Flg.
	 */
	public static enum USER_PERMISSION {
		/** 无权限者 不可读. */
		NOT_READ("0"),
		/** 参加者 只读. */
		READ_ONLY("1"),
		/** 申请者 可改. */
		WRITE("2");

		private String value;

		USER_PERMISSION(String value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "USER_PERMISSION";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public String value() {
			return this.value;
		}
	}

	/**
	 * 周期会议修改标志.
	 */
	public static enum CYCLEMET_MODIFY {

		/** 未修改. */
		FALSE(0),
		/** 修改过. */
		TRUE(1);

		private Integer value;

		CYCLEMET_MODIFY(Integer value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "CYCLEMET_MODIFY";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public Integer value() {
			return this.value;
		}
	}
	
	/**
	 * 是否通知参加者.
	 */
	public static enum CONTACT_USER {
		
		/** 未修改. */
		FALSE("0"),
		/** 修改过. */
		TRUE("1");
		
		private String value;
		
		CONTACT_USER(String value) {
			this.value = value;
		}
		
		public static String getEnumName() {
			return "CONTACT_USER";
		}

		public String value() {
			return this.value;
		}
	}

	/**
	 * 周期会议删除标志.
	 */
	public static enum CYCLEMET_DELETE {

		/** 未删除. */
		FALSE(0),
		/** 已删除. */
		TRUE(1);

		private Integer value;

		CYCLEMET_DELETE(Integer value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "CYCLEMET_DELETE";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public Integer value() {
			return this.value;
		}
	}

	/**
	 * 周期会议类型为每月时,提前后延的标识.
	 */
	public static enum DELAY_FLG {
		/** 后延. */
		AFTER("0"),
		/** 提前. */
		BEFORE("1");

		private String value;

		DELAY_FLG(String value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "DELAY_FLG";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public String value() {
			return this.value;
		}
	}

	/** 会议开始最晚小时. */
	public static final int MEET_END_HOUR_ST = 17;
	/** 会议结束最晚小时. */
	public static final int MEET_END_HOUR_EN = 18;
	/** 会议开始最早小时. */
	public static final int MEET_START_HOUR = 8;
	/** 30分钟定数. */
	public static final int MINUTE_30 = 30;
	/** Yc0040一页显示的数据件数 */
	public static final int YC0040_PAGE_COUNT = 18;

	/**
	 * 会议室预约分钟间隔
	 */
	public static enum MEET_MINUTES {
		/** 后延. */
		M_00("00"),
		/** 提前. */
		M_30("30");

		private String value;

		MEET_MINUTES(String value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "MEET_MINUTES";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public String value() {
			return this.value;
		}
	}
	/**
	 * 工作日.
	 */
	public static enum WEEK_DAY {
		/** 周一. */
		MON("周一"),
		/** 星期二. */
		TUE("周二"),
		/** 周三. */
		WEN("周三"),
		/** 周四. */
		THU("周四"),
		/** 周五. */
		FRI("周五"),
		/** 周六. */
		SAT("周六"),
		/** 周日. */
		SUN("周日"),
		/** 空字符串. */
		ERRORDAY("");
		private String value;

		WEEK_DAY(String value) {
			this.value = value;
		}

		public static String getEnumName() {
			return "WORK_DAY";
		}

		@Override
		public String toString() {
			return String.valueOf(this.value);
		}

		public String value() {
			return this.value;
		}
	}	
}
