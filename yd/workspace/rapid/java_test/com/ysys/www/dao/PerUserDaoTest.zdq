/*
 * Powered By [rapid-framework]
 * Web Site: http://www.rapid-framework.org.cn
 * Google Code: http://code.google.com/p/rapid-framework/
 */

package com.ysys.www.dao;

import java.util.List;

import javax.annotation.Resource;
import org.junit.Test;
import static junit.framework.Assert.*;

import java.util.*;

import javacommon.base.*;
import javacommon.util.*;

import cn.org.rapid_framework.util.*;
import cn.org.rapid_framework.web.util.*;
import cn.org.rapid_framework.page.*;
import cn.org.rapid_framework.page.impl.*;

import com.ysys.www.model.*;
import com.ysys.www.dao.*;
import com.ysys.www.service.*;

/**
 * @author badqiu email:badqiu(a)gmail.com
 * @version 1.0
 * @since 1.0
 */


public class PerUserDaoTest extends BaseDaoTestCase{
	
	private PerUserDao dao;
	
	/** autowire by name */
	@Resource
	public void setPerUserDao(PerUserDao dao) {
		this.dao = dao;
	}

	@Override
	protected String[] getDbUnitDataFiles() {
		return new String[]{"classpath:common_testdata.xml","classpath:PerUser_testdata.xml"};
	}
	
	@Test
	public void findByPageRequest() {
		int pageNumber = 1;
		int pageSize = 10;
		
		PageRequest<Map> pageRequest = new PageRequest(new HashMap());
		pageRequest.setPageNumber(pageNumber);
		pageRequest.setPageSize(pageSize);
		pageRequest.setSortColumns(null);
		
		pageRequest.getFilters().put("empPnm", "1");
		pageRequest.getFilters().put("empCnm", "1");
		pageRequest.getFilters().put("empJnm", "1");
		pageRequest.getFilters().put("password", "1");
		pageRequest.getFilters().put("deptId", "1");
		pageRequest.getFilters().put("startDate", "1");
		pageRequest.getFilters().put("empPwdask", "1");
		pageRequest.getFilters().put("empPwdasw", "1");
		pageRequest.getFilters().put("empCookie", "1");
		pageRequest.getFilters().put("empWorkid", "1");
		pageRequest.getFilters().put("empWorkpwd", "1");
		pageRequest.getFilters().put("higherId", "1");
		pageRequest.getFilters().put("ohigherId", "1");
		pageRequest.getFilters().put("contractId", "1");
		pageRequest.getFilters().put("endDate", "1");
		pageRequest.getFilters().put("yxNo", "1");
		pageRequest.getFilters().put("yxContractId", "1");
		pageRequest.getFilters().put("yjsr", "1");
		pageRequest.getFilters().put("empDesp", "1");
		pageRequest.getFilters().put("empFlag", "1");
		pageRequest.getFilters().put("updatetime", "1");
		
		Page page = dao.findByPageRequest(pageRequest);
		
		assertEquals(pageNumber,page.getThisPageNumber());
		assertEquals(pageSize,page.getPageSize());
		List resultList = (List)page.getResult();
		assertNotNull(resultList);
		
	}
	
}
