<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Tuser">

    <typeAlias alias="tuser" type="com.ysys.www.model.Tuser"/>

    <resultMap id="tuserResult" class="tuser">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="createTime" column="createTime"/>
        <result property="expireTime" column="expireTime"/>
        <result property="person" column="person"/>
    </resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="commonColumns">
	    <![CDATA[
        	id ,
        	username ,
        	password ,
        	createTime ,
        	expireTime ,
        	person 
	    ]]>
	</sql>
	
    <insert id="insert" parameterClass="tuser">
    <![CDATA[
        INSERT INTO
        t_user (
        	id ,
        	username ,
        	password ,
        	createTime ,
        	expireTime ,
        	person 
        ) VALUES (
        	#id# ,
        	#username# ,
        	#password# ,
        	#createTime# ,
        	#expireTime# ,
        	#person# 
        )
    ]]>
    <!--
    mysql:		type="post" SELECT  select last_insert_id()
    sqlserver:	type="post" SELECT  @@identity  AS  ID
    oracle:		type="pre" 	SELECT  sequenceName.nextval AS ID FROM DUAL
    DB2:		type="pre"	values nextval for sequenceName
    -->
        <selectKey resultClass="java.lang.Long" type="post" keyProperty="id" >
            select last_insert_id()
        </selectKey>
    </insert>

    <update id="update" parameterClass="tuser">
    <![CDATA[
        UPDATE t_user SET
	        username = #username# ,
	        password = #password# ,
	        createTime = #createTime# ,
	        expireTime = #expireTime# ,
	        person = #person# 
        WHERE 
	        id = #id# 
    ]]>
    </update>

    <delete id="delete" parameterClass="java.lang.Integer">
    <![CDATA[
        delete from t_user where
        id = #id# 
    ]]>
    </delete>
    
    <select id="getById" resultMap="tuserResult">
	    select <include refid="commonColumns"/>
	    <![CDATA[
	        from t_user 
	        where 
		        id = #id# 
	    ]]>
    </select>

	<sql id="dynamicWhere">
	    <dynamic prepend="WHERE">
	       <isNotEmpty prepend="AND" property="id">
	             id = #id#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="username">
	             username = #username#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="password">
	             password = #password#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="createTime">
	             createTime = #createTime#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="expireTime">
	             expireTime = #expireTime#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="person">
	             person = #person#
	       </isNotEmpty>
	    </dynamic>	
	</sql>
	 
    <select id="count" resultClass="long">
    <![CDATA[
        select count(*) from t_user
    ]]>
		<include refid="dynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 #offset#,#pageSize#,#lastRows# 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="pageSelect" resultMap="tuserResult">
    	select <include refid="commonColumns"/>
	    <![CDATA[
	        from t_user 
	    ]]>
		<include refid="dynamicWhere"/>
		<dynamic prepend="ORDER BY">
			<isNotEmpty property="sortColumns">$sortColumns$</isNotEmpty>
		</dynamic>
    </select>

    <select id="getByUsername" resultMap="tuserResult" parameterClass="java.lang.String">
	    select <include refid="commonColumns"/>
	    <![CDATA[
	        from t_user where username = #username#
	    ]]>
    </select>
    
    <select id="getByPerson" resultMap="tuserResult" parameterClass="java.lang.Integer">
	    select <include refid="commonColumns"/>
	    <![CDATA[
	        from t_user where person = #person#
	    ]]>
    </select>
    
	
</sqlMap>
