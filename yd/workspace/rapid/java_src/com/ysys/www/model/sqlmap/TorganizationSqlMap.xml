<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Torganization">

    <typeAlias alias="torganization" type="com.ysys.www.model.Torganization"/>

    <resultMap id="torganizationResult" class="torganization">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sn" column="sn"/>
        <result property="description" column="description"/>
        <result property="pid" column="pid"/>
    </resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="commonColumns">
	    <![CDATA[
        	id ,
        	name ,
        	sn ,
        	description ,
        	pid 
	    ]]>
	</sql>
	
    <insert id="insert" parameterClass="torganization">
    <![CDATA[
        INSERT INTO
        t_organization (
        	id ,
        	name ,
        	sn ,
        	description ,
        	pid 
        ) VALUES (
        	#id# ,
        	#name# ,
        	#sn# ,
        	#description# ,
        	#pid# 
        )
    ]]>
    <!--
    mysql:		type="post" SELECT  select last_insert_id()
    sqlserver:	type="post" SELECT  @@identity  AS  ID
    oracle:		type="pre" 	SELECT  sequenceName.nextval AS ID FROM DUAL
    DB2:		type="pre"	values nextval for sequenceName
    -->
        <selectKey resultClass="java.lang.Long" type="post" keyProperty="id" >
            select last_insert_id()
        </selectKey>
    </insert>

    <update id="update" parameterClass="torganization">
    <![CDATA[
        UPDATE t_organization SET
	        name = #name# ,
	        sn = #sn# ,
	        description = #description# ,
	        pid = #pid# 
        WHERE 
	        id = #id# 
    ]]>
    </update>

    <delete id="delete" parameterClass="java.lang.Integer">
    <![CDATA[
        delete from t_organization where
        id = #id# 
    ]]>
    </delete>
    
    <select id="getById" resultMap="torganizationResult">
	    select <include refid="commonColumns"/>
	    <![CDATA[
	        from t_organization 
	        where 
		        id = #id# 
	    ]]>
    </select>

	<sql id="dynamicWhere">
	    <dynamic prepend="WHERE">
	       <isNotEmpty prepend="AND" property="id">
	             id = #id#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="name">
	             name = #name#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="sn">
	             sn = #sn#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="description">
	             description = #description#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="pid">
	             pid = #pid#
	       </isNotEmpty>
	    </dynamic>	
	</sql>
	 
    <select id="count" resultClass="long">
    <![CDATA[
        select count(*) from t_organization
    ]]>
		<include refid="dynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 #offset#,#pageSize#,#lastRows# 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="pageSelect" resultMap="torganizationResult">
    	select <include refid="commonColumns"/>
	    <![CDATA[
	        from t_organization 
	    ]]>
		<include refid="dynamicWhere"/>
		<dynamic prepend="ORDER BY">
			<isNotEmpty property="sortColumns">$sortColumns$</isNotEmpty>
		</dynamic>
    </select>

	
</sqlMap>
