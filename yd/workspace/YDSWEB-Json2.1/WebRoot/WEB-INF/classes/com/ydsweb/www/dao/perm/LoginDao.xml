<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTDSQLMap2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="LoginDao">
	<typeAlias alias="userResult" type="com.ydsweb.www.dao.condition.perm.UserResult"/>

	<!-- 登录 --> 
	<select id="login" parameterClass="String" resultClass="userResult">  
		select emp_id as userId, emp_name as userName, emp_passwd as userPassword, emp_jobid  as userJobId
		from tempinfo t 
		where emp_id = #userId# and emp_Flag = '0';
	</select>  
</sqlMap>  
