<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Book">

    <typeAlias alias="book" type="com.ysys.www.model.Book"/>

    <resultMap id="bookResult" class="book">
        <result property="id" column="ID"/>
        <result property="name" column="NAME"/>
        <result property="isbm" column="ISBM"/>
        <result property="author" column="AUTHOR"/>
    </resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="commonColumns">
	    <![CDATA[
        	ID ,
        	NAME ,
        	ISBM ,
        	AUTHOR 
	    ]]>
	</sql>
	
    <insert id="insert" parameterClass="book">
    <![CDATA[
        INSERT INTO
        book (
        	ID ,
        	NAME ,
        	ISBM ,
        	AUTHOR 
        ) VALUES (
        	#id# ,
        	#name# ,
        	#isbm# ,
        	#author# 
        )
    ]]>
    <!--
    mysql:		type="post" SELECT  select last_insert_id()
    sqlserver:	type="post" SELECT  @@identity  AS  ID
    oracle:		type="pre" 	SELECT  sequenceName.nextval AS ID FROM DUAL
    DB2:		type="pre"	values nextval for sequenceName
    -->
        <selectKey resultClass="java.lang.Long" type="post" keyProperty="id" >
            select last_insert_id()
        </selectKey>
    </insert>

    <update id="update" parameterClass="book">
    <![CDATA[
        UPDATE book SET
	        NAME = #name# ,
	        ISBM = #isbm# ,
	        AUTHOR = #author# 
        WHERE 
	        ID = #id# 
    ]]>
    </update>

    <delete id="delete" parameterClass="java.lang.Double">
    <![CDATA[
        delete from book where
        ID = #id# 
    ]]>
    </delete>
    
    <select id="getById" resultMap="bookResult">
	    select <include refid="commonColumns"/>
	    <![CDATA[
	        from book 
	        where 
		        ID = #id# 
	    ]]>
    </select>

	<sql id="dynamicWhere">
	    <dynamic prepend="WHERE">
	       <isNotEmpty prepend="AND" property="id">
	             ID = #id#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="name">
	             NAME = #name#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="isbm">
	             ISBM = #isbm#
	       </isNotEmpty>
	       <isNotEmpty prepend="AND" property="author">
	             AUTHOR = #author#
	       </isNotEmpty>
	    </dynamic>	
	</sql>
	 
    <select id="count" resultClass="long">
    <![CDATA[
        select count(*) from book
    ]]>
		<include refid="dynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 #offset#,#pageSize#,#lastRows# 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="pageSelect" resultMap="bookResult">
    	select <include refid="commonColumns"/>
	    <![CDATA[
	        from book 
	    ]]>
		<include refid="dynamicWhere"/>
		<dynamic prepend="ORDER BY">
			<isNotEmpty property="sortColumns">$sortColumns$</isNotEmpty>
		</dynamic>
    </select>

	
</sqlMap>
